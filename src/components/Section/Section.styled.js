import styled from 'styled-components';

const StyledSectionContainer = styled.div`
  padding-left: 50px;
`;

const StyledTitle = styled.h2`
  padding-top: 10px;
`;

export {StyledSectionContainer, StyledTitle}